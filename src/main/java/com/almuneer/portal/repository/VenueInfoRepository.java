package com.almuneer.portal.repository;

import com.almuneer.portal.model.VenueInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VenueInfoRepository extends JpaRepository<VenueInfo, Integer> {
}
